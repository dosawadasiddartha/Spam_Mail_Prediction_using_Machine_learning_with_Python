# Spam_Mail_Prediction_using_Machine_learning_with_Python
This project demonstrates how to build a machine learning model that can accurately classify emails as Spam or Not Spam using Python. It includes data preprocessing, feature extraction using TF-IDF, model training, evaluation, and prediction on new email samples.

How to Run:

1) Clone the repository
2) Install dependencies
3) Run the VS code 
